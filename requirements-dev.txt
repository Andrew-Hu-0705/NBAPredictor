# Test-only dependencies, on top of requirements.txt. Not part of the API
# serving image (requirements-api.txt) — tests run against source, not the
# built container.
pytest>=8.0.0
httpx>=0.27.0

# Load testing (Stage 6) — a dev tool you run against a live instance, not a
# runtime dependency of the service itself.
locust==2.46.3
