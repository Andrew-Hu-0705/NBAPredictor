nba_api>=1.4.1
matplotlib>=3.7.0
Flask>=3.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
mlflow==2.17.2
prometheus-fastapi-instrumentator==7.1.0

# Pinned exactly (not >=), matching requirements-api.txt: these end up inside
# the pickled model/explainer object graph, so a floating version here can
# silently retrain something the serving image can no longer unpickle. See
# the comment in requirements-api.txt for what that looks like in practice.
numpy==1.26.4
pandas==2.0.3
xgboost==2.1.4
scikit-learn==1.3.2
numba==0.61.0
llvmlite==0.44.0
shap==0.44.1
joblib==1.4.2